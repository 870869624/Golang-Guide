# 例如原本所有数据都在/home/topsec/data 那么就把这个目录直接整个复制到 /data/（数据盘，服务停止再迁移数据）
现迁移 然后卸载重装

```yaml
## 主要就是这个custom-values.yaml里面的路径指向你新的路径
## @param storage.localPath Local path for local-path storage class
localPath: /home/topsec/data/ 
```
