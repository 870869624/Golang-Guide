## 1. 查看所有clickhouse的应用并查看所处位置

```bash
[root@master ~]# kubectl get all -n topsec-topngdlp | grep -i clickhouse

pod/clickhouse-0                     1/1     Running     0               11d
service/clickhouse    NodePort   10.43.147.100   <none>        8123:30123/TCP,9000:30900/TCP                                   492d
statefulset.apps/clickhouse   1/1     49d
```

## 2. 直接进入clickhouse并查询数据

```bash
kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "SELECT * FROM sensitive_file WHERE id = '5213c27288c801a2bc75f5fd4b9a3124';"
```

## 3. 查看当前表活跃的parts数量

```bash
kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "SELECT count() FROM system.parts WHERE table = 'sensitive_file' AND active = 1"
```

## 4. 手动触发合并 (这是一个异步操作，执行后会加入合并队列)

```bash
kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "OPTIMIZE TABLE sensitive_file FINAL;
"
```

### 4.1 可能会遇到

```bash
[root@master ~]# kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "OPTIMIZE TABLE sensitive_file FINAL;
> "
Received exception from server (version 22.8.8):
Code: 214. DB::Exception: Received from localhost:9000. DB::ErrnoException. DB::ErrnoException: Could not calculate available disk space (statvfs), errno: 2, strerror: No such file or directory. (CANNOT_STATVFS)
(query: OPTIMIZE TABLE sensitive_file FINAL;
)
command terminated with exit code 214
```

#### 4.1.1 错误原因分析

- ClickHouse 在执行 OPTIMIZE 等数据合并操作之前，需要检查目标表的存储路径是否有足够磁盘空间。它通过 statvfs 系统调用来获取路径所在文件系统的信息。
- errno 2 表示“No such file or directory”，说明传递给 statvfs 的路径在文件系统中找不到。
- 在 Kubernetes 环境中，最可能的原因是：

  - ClickHouse 的数据目录（默认 /var/lib/clickhouse）或临时目录（tmp_path）挂载的持久卷（PVC）未正确挂载。
  - Pod 启动后，持久卷的挂载点丢失（例如容器重启后 PVC 未能重新挂载，或挂载配置错误）。
  - ClickHouse 配置文件中指定的某个磁盘（如 storage_configuration 中的自定义路径）不存在。

## 5. 查看容器内磁盘挂载情况
```bash
kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- df -h
```

## 6. 查看块限制数

```bash
kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "SHOW CONFIG LIKE 'max_parts_in_total';"
```

## 7.重载配置
```bash
[root@master ~]# kubectl exec -it -n topsec-topngdlp statefulset.apps/clickhouse -- clickhouse-client --password TopsecCdu_1130 --query "SYSTEM RELOAD CONFIG"
```

## 8. 修改配置
```bash
[root@master ~]# kubectl edit configmap -n topsec-topngdlp clickhouse-config
```

## 9. 这个命令的目的是：查看 ClickHouse 数据库中 max_partitions_to_read 这个性能限制参数的当前配置值。
```bash
#这个查询的作用是：
#从 system.settings 系统表中查询
#查找 设置项名称 为 max_partitions_to_read 的配置
#返回该设置的所有信息（值、描述、默认值等）
#max_partitions_to_read 是什么？
#这是 ClickHouse 的一个重要性能参数，用于限制：
#单次查询最多可以读取的 分区数量
#防止查询扫描过多分区导致性能问题
#默认值通常是 1000

[root@master ~]# kubectl exec -it -n topsec-topngdlp  statefulset.apps/clickhouse  -- clickhouse-client --password TopsecCdu_1130 --query "select * from system.settings where name = 'max_partitions_to_read';"
max_partitions_to_read  -1      0       Limit the max number of partitions that can be accessed in one query. <= 0 means unlimited.     \N      \N      0       Int64
```
